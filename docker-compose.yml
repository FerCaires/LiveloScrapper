networks:
  livelo-network:
    driver: bridge

services:
  scraper:
    build: .
    container_name: livelo-scrapper
    environment:
      - TZ=America/Sao_Paulo
    volumes:
      - ./data:/app/data
    networks:
      - livelo-network
    restart: unless-stopped
